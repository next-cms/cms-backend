import React from "react";
import { Layout } from "antd";

const { Header, Content, Footer, Sider } = Layout;
const LayoutWithSider = ({ header, sider, children }) => {
    return (
        <Layout>
            {header}
            <Layout>
                <Sider>
                    {sider}
                </Sider>
            </Layout>
            <Layout>
                <Content>{children}</Content>
            </Layout>
        </Layout>
    );
};
export default LayoutWithSider;
